# OutSystems

It seems that if a developer doesn't have a proper Organization scope Role for a Library with a new Version, they he/she cannot see the newer Version in "Review library updates" dialogl
